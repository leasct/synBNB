{% extends "base.html.twig" %}
{% block title %} modifier le password{% endblock %}

{% block body %}
<div class="container">
    <h1>Modification du mot de passe</h1>
    {{form_start(form)}}

    {{form_widget(form)}}

    <button type="submit" class="btn btn-primary">Modifiez le mot de passe</button>

    {{form_end(form)}}
</div>

{% endblock %}